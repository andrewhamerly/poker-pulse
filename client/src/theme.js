import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    brand: {
      dark: 'rgb(53, 59, 59)',
      red: 'rgb(218, 52, 45)',
    },
  },
});

export default theme;